<Window
    x:Class="Inventory.View.Edit.Tables.Employees.PostEditWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Inventory.View.Edit"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    x:Name="EditPostWindow"
    Title="Изменить должность"
    Width="300"
    Height="163"
    ResizeMode="CanMinimize"
    WindowStartupLocation="CenterScreen"
    WindowStyle="ThreeDBorderWindow"
    mc:Ignorable="d">


    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" MinHeight="20" />
        </Grid.RowDefinitions>

        <StackPanel
            Grid.Row="0"
            Grid.Column="0"
            Grid.ColumnSpan="2"
            Width="250"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Orientation="Vertical"
            Style="{StaticResource StackPanel}">
            <TextBlock
                Padding="3"
                Style="{StaticResource TextBlocks}"
                Text="Название отдела:" />
            <TextBox
                Padding="3"
                MaxLength="100"
                Style="{StaticResource TextBoxs}"
                Text="{Binding Post.Name, UpdateSourceTrigger=PropertyChanged}" />
        </StackPanel>

        <StackPanel
            Grid.Row="1"
            Grid.Column="0"
            Grid.ColumnSpan="2"
            Style="{StaticResource StackPanel}">
            <Button
                HorizontalAlignment="Left"
                Command="{Binding Edit}"
                CommandParameter="{Binding ElementName=EditPostWindow}"
                Content="Изменить"
                Style="{StaticResource Buttons}" />
        </StackPanel>

        <StackPanel
            Grid.Row="1"
            Grid.Column="1"
            HorizontalAlignment="Right"
            Style="{StaticResource StackPanel}">
            <Button
                Command="{Binding Cancel}"
                CommandParameter="{Binding ElementName=EditPostWindow}"
                Content="Отмена"
                Style="{StaticResource Buttons}" />
        </StackPanel>

    </Grid>
</Window>
